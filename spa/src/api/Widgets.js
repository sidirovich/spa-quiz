import { ServerLink } from '../config/';

const getWidgets = async (jwt, id) => {

};

const createWidget = async (jwt, body) => {

};

const updateWidget = async (jwt, body) => {

};

const deleteWidget = async (jwt, id) => {

};

export default {
    getWidgets,
    createWidget,
    updateWidget,
    deleteWidget,
}